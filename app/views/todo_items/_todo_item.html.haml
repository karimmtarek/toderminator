%li{id:"todo-#{todo_item.id}"}
  = todo_item.name
  = form_for [@list, todo_item] do |f|
    = f.check_box :completed

  = link_to 'complete', list_todo_item_path(@list, todo_item), method: :delete, remote: true, data: { confirm: 'Is it really completed?'}
